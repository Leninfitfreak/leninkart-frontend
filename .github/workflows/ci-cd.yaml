name: Deploy Frontend (Docker Hub + GitOps)

on:
  push:
    branches:
      - dev
      - staging
      - main
  workflow_dispatch:

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

env:
  SERVICE_NAME: frontend
  IMAGE_NAME: frontend
  INFRA_REPO: Leninfitfreak/leninkart-infra

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - name: Resolve image tag
        id: meta
        run: |
          echo "image_tag=${{ github.run_id }}" >> $GITHUB_OUTPUT

  build_image:
    runs-on: ubuntu-latest
    needs: prepare
    outputs:
      image_tag: ${{ needs.prepare.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | \
          docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build & Push Frontend Image
        run: |
          IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.prepare.outputs.image_tag }}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "✅ Pushed image: $IMAGE"

  promote_release:
    runs-on: ubuntu-latest
    needs: build_image
    permissions:
      contents: write
    steps:
      - name: Select infra branch & values file
        run: |
          BRANCH="${GITHUB_REF##*/}"
          if [[ "$BRANCH" == "main" ]]; then
            echo "INFRA_BRANCH=main" >> "$GITHUB_ENV"
            echo "VALUES_FILE=applications/frontend/helm/values-prod.yaml" >> "$GITHUB_ENV"
          elif [[ "$BRANCH" == "staging" ]]; then
            echo "INFRA_BRANCH=staging" >> "$GITHUB_ENV"
            echo "VALUES_FILE=applications/frontend/helm/values-staging.yaml" >> "$GITHUB_ENV"
          else
            echo "INFRA_BRANCH=dev" >> "$GITHUB_ENV"
            echo "VALUES_FILE=applications/frontend/helm/values-dev.yaml" >> "$GITHUB_ENV"
          fi

      - name: Checkout infra repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.INFRA_REPO }}
          ref: ${{ env.INFRA_BRANCH }}
          token: ${{ secrets.INFRA_PAT }}
          path: infra

      - name: Install yq
        uses: mikefarah/yq@v4

      - name: Update Helm values
        run: |
          cd infra
          echo "📁 Updating: $VALUES_FILE"
          echo "📦 Image: ${{ secrets.DOCKERHUB_USERNAME }}/frontend:${{ needs.build_image.outputs.image_tag }}"
          yq -i '.image.repository = "${{ secrets.DOCKERHUB_USERNAME }}/frontend"' "$VALUES_FILE"
          yq -i '.image.tag = "${{ needs.build_image.outputs.image_tag }}"' "$VALUES_FILE"
          echo "✅ Updated $VALUES_FILE"

      - name: Commit & push changes
        run: |
          cd infra
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@users.noreply.github.com"
          git add "$VALUES_FILE"
          git commit -m "deploy(frontend): run-${{ needs.build_image.outputs.image_tag }}" || echo "No changes"
          git push origin "$INFRA_BRANCH"
